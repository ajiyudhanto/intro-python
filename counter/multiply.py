# with __init__, a folder is treated as package
# inside package, .py is treated as modules
# package can be accessed outside folder (in this case in app.py)

def multiply (number_1, number_2):
    return number_1 * number_2